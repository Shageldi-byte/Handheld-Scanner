pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        jcenter()
        maven { url "https://jitpack.io" }
        maven { url "https://repo.repsy.io/mvn/handheldgroup/handheldgroup" }
        flatDir {
            dirs 'honeywell'
        }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter()
        maven { url "https://jitpack.io" }
        maven { url "https://repo.repsy.io/mvn/handheldgroup/handheldgroup" }
        flatDir {
            dirs 'honeywell'
        }
    }
}
rootProject.name = "HandHeld"
include ':app'
